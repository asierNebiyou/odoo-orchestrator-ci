{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Permissions for the main shell window. Deliberately minimal — the technical-design doc's plugin sandboxing model means a future plugin webview gets its OWN capability file granting only what its manifest declares, never this one.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
